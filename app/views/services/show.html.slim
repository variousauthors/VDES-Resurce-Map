.page-header
  h1 Service

dl

/- if can?(:edit, @service)
= link_to text_with_icon('Edit', 'edit'), edit_service_path(@service), class: 'btn btn-default'
'
= link_to text_with_icon('Back', 'chevron-left'), :back, class: 'btn btn-default'

/- if can?(:destroy, @service)
'
= link_to text_with_icon('Destroy', 'remove'), service_path(@service),
          method: :delete, data: { confirm: "Are you sure?" }, class: 'btn btn-danger'
